package org.campagnelab.bdval.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class evaluateType_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<evaluateType> constants = ListSequence.fromList(evaluateType.getConstants()).iterator();
    while (constants.hasNext()) {
      evaluateType constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<evaluateType> constants = ListSequence.fromList(evaluateType.getConstants()).iterator();
    while (constants.hasNext()) {
      evaluateType constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    evaluateType constant = evaluateType.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
